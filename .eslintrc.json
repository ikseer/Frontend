{
  "extends": ["next/core-web-vitals"],
  "rules": {
    "no-var": "error",
    "quotes": ["error", "single"],
    "camelcase": ["error", { "properties": "never" }],
    "no-unused-vars": [
      "error",
      { "varsIgnorePattern": "^_", "argsIgnorePattern": "^_" }
    ]
  }
}
